import React from 'react';
import { useParams } from 'react-router-dom';

const OrderDetail = () => {
    let { id } = useParams();
    return (
        <div>

        </div>
    );
};

export default OrderDetail

